import Gameboard from "./gameboard";

class Interface {
  constructor(playerBoard, computerBoard) {
    this.playerReferenceBoard = playerBoard;
    this.computerReferenceBoard = computerBoard;
  }
  renderBoard(board) {
    
  }
}

export default Interface